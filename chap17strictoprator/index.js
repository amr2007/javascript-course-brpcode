// == Assigment oprator 
// Comparison oprator (compare if values are euqal)
// === Strict equality oprator (compare if values & datatypes are equal)
// != inequality oprator
// !== strict inequality oprator

const PI = "3.14";

if (PI !== "3.14") {
    console.log("That is NOT Pi");
}
else {
    console.log("That is Pi");
}